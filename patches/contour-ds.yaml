apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: envoy
  namespace: projectcontour
spec:
  template:
    spec:
      tolerations:
      - key: "specialPodsOnly"
        value: "please"
        operator: "Equal"
        effect: "NoSchedule"
